secretsNames:
  # Left side: the id the workflow asks for with runtime.getSecret().
  # Right side: where the CLI finds the value locally. Deliberately not the
  # same string — a secret whose id equals its env var name is easy to shadow
  # by accident, and the CLI warns about exactly that.
  RISK_API_KEY:
    - CRE_RISK_API_KEY
